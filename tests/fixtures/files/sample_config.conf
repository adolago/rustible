# Sample configuration file for testing
# This file is used by copy module tests

[general]
name = sample
version = 1.0
enabled = true

[network]
host = 0.0.0.0
port = 8080
timeout = 30

[logging]
level = info
file = /var/log/sample.log
