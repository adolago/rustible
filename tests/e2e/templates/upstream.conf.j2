# Upstream server configuration
# Generated by Rustible E2E tests

upstream backend {
    least_conn;
{% for server in upstream_servers %}
    server {{ server }}:{{ upstream_port | default(8080) }} weight=1;
{% endfor %}
}
